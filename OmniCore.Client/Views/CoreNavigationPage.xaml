<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
            xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
            Padding="0,40,0,0"
            IconImageSource="hamburger.png"
            Title="Omnicore"
            x:Class="OmniCore.Client.Views.CoreNavigationPage"
            xmlns:vm="clr-namespace:OmniCore.Client.ViewModels"
             >
    <CollectionView SelectionChangedCommand="{Binding FlyoutSelectionChangedCommand}" ItemsSource="{Binding FlyoutItems}">
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="vm:FlyoutItemModel">
                        <Grid Padding="5,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="30"/>
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Image Source="{Binding Icon}" />
                            <Label Grid.Column="1"
                                    Margin="20,0"
                                    Text="{Binding Title}"
                                    FontSize="20"
                                    FontAttributes="Bold"
                                    VerticalOptions="Center" />
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </ContentPage>